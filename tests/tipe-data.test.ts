describe('Data Type', function () {
    it('should must declare', function () {
        const name: string = "Dewa Gede Agung Ari"
        const balance: number = 10000000
        const isVip: boolean = true

        console.info(name)
        console.info(balance)
        console.info(isVip)
    })
})