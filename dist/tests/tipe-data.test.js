"use strict";
describe('Data Type', function () {
    it('should must declare', function () {
        const name = "Dewa Gede Agung Ari";
        const balance = 10000000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
